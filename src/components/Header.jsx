import React from "react";

// const Header = (props) => {
//   return <h1>{props.titulo}</h1>;
// };

// Destruccioring
const Header = ({titulo}) => {
  return <h1>{titulo}</h1>;
};

export default Header;
